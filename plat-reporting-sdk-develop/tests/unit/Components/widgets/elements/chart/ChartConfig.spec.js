import makeChartTableConfig from '@/components/widgets/elements/chart/ChartConfig.js'

describe('ChartConfig.js', () => {
  it('check makeChartTableConfig', () => {
    // excute
    // makeTableDefaultConfig([{}])
  })
})
