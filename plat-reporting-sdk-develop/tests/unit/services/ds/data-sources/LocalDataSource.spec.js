import LocalDataSource from '@/services/ds/data-sources/LocalDataSource.js'

describe('LocalDataSource.js', () => {
  it('check method query', () => {
    // excute
    // LocalDataSource.query({filter: undefined})
  })

  it('check method total', () => {
    // excute
    // LocalDataSource.total()
  })
})
