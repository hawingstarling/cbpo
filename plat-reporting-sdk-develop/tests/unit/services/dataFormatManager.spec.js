import DataFormatManager from '@/services/dataFormatManager.js'

describe('dataFormatManager', () => {
  it('Check class DataFormatManager', () => {
    // excute
    // DataFormatManager.create()
    // DataFormatManager.create({ common: '' })
    // DataFormatManager.create({ common: {} })
    // DataFormatManager.format()
    // DataFormatManager.formatAggr()
    // DataFormatManager.createAggrFormat(1, 1, 1, 1, '')
  })
})
