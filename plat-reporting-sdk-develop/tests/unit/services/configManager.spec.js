import ConfigManager from '@/services/configManager.js'

describe('ConfigManager', () => {
  it('Check class ConfigManager', async () => {
    // setup
    var dataSource = {}
    // excute
    ConfigManager.get('dataSource')
  })
})
