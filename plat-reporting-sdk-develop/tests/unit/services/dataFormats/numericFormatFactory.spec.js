import numericFormatFactory from '@/services/dataFormats/numericFormatFactory.js'

describe('numericFormatFactory', () => {
  it('Check funtional formalt numericFormatFactory', () => {
    // excute
    // numericFormatFactory()
    // numericFormatFactory({siPrefix: true})
    // numericFormatFactory({precision: true})
    // numericFormatFactory({siPrefix: false})
  })
})
